class DayModel {
  constructor(hours, date, icon, tempmax, tempmin) {
    this.hours = hours;
    this.date = date;
    this.icon = icon;
    this.tempmax = tempmax;
    this.tempmin = tempmin;
  }
}

module.exports = DayModel;
